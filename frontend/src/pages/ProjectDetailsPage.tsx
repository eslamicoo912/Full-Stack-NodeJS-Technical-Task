import TaskBoard from '../features/tasks/TaskBoard';

// The project detail page is just a wrapper around the task board
function ProjectDetailPage() {
  return <TaskBoard />;
}

export default ProjectDetailPage;
